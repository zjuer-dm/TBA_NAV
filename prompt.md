这里，external文件夹下面是我觉得有用的项目代码，可以用来参考。首先，这里的external/NAV_vlm就是以前我自己写的，就是按照我的思路，使用COT的数据让VLM学会思考和拓扑图的选择。这里主要就是我们原本的所有思路的实现，但是我的代码不是特别的好，可能有的地方不够好，所以训练的效果不是很突出。这里external/ETP-R1是一个基于拓扑图的方法，我觉得因为我们要做拓扑图的话，我们可以大量的参考一下里面参数的设定。因为stream VLN是视频流，很多的参数不适合现在的思路。不过要注意的是external/ETP-R1不是基于VLM的方法。其他2个项目暂时不用看。

